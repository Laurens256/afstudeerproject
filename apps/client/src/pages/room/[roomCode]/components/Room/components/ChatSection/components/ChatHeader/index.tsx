import classes from './ChatHeader.module.css';

// TODO: button to toggle notification ping, button to view users
const ChatHeader = () => {
	console;

	return (
		<header className={classes.header}>
			<h2>Game chat</h2>
		</header>
	);
};

export default ChatHeader;
